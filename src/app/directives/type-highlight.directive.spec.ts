import { TypeHighlightDirective } from './type-highlight.directive';

describe('TypeHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
